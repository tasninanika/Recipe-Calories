import PropTypes from 'prop-types';

const Recipe = ({recipe}) => {
    return (
        <div>
            
        </div>
    );
};

Recipe.propTypes = {
    recipe: PropTypes.object.isRequired
    
}

export default Recipe;